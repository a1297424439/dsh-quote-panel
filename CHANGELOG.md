# Changelog

## 2.0.0 (2026-08-21)

- 🚀 **V2.0 正式发布**：从 v1.4.3 起的多项修复与打磨汇总，功能稳定、文档齐备。
- 🐛 **修复对话气泡高度塌缩**：`.dshq-bubble` 同时设置 `top:0` 与 JS 注入的 `bottom` 导致绝对定位高度被压成 0（文字全部溢出框外），删除 `top:0` 后气泡正确包裹两行文字。
- 🐛 **修复气泡宽度适配**：内容改为 `white-space:nowrap`，气泡宽度自动贴合最长文字行；牛位于屏幕右侧时不再因可用宽度收缩而换行溢出。
- 🐛 **修复气泡遮挡牛头**：气泡底边从 `+140px` 上移到 `+228px`，高于牛喊牛来跳跃动画的最高点（`+204px`），牛跳跃不再顶进气泡。
- 🐛 **修复自选列表未持久化**：面板 add/remove 自选时同步写入 `localStorage`（`dshq.lists.cn/us`），弹窗版/桌面版桌宠能读到用户自选，不再固定播报三大指数。
- 🎯 **播报跟随选中标的**：点某只股票看 K 线时，牛来只播报该标的；未选中时播报整个自选列表。
- 🧹 **精简 UI**：移除会话头部独立的「🐂 牛来」按钮，牛来开关统一收敛到行情看板内。
- 📄 **文档完善**：中英文 README 补齐徽章、功能清单、FAQ、贡献指南与路线图；`desktop-pet` 元数据清理占位符。

## 1.4.3 (2026-08-21)

- ✨ **DSH 内嵌牛补齐修仙特效**：1.4.2 只搬了瞬移+气浪圈，本版把桌面版的**残影 + 仙气**完整移植到看板内嵌版。
  - 瞬移时沿轨迹摆 **4 道残影**（模糊渐隐，自动跟随朝向镜像）。
  - 落地时 **8 颗仙气粒子 + 气浪圈**爆发；喊 `\牛来/` 时升级为 **14 颗金光仙气**。
  - 特效层用 DOM 直插（不触发 React re-render），随牛所在 stage 挂载/卸载。

## 1.4.2 (2026-08-21)

- ✨ **DSH 看板内嵌牛替换为真牛瞬移版**：告别第一版的自绘拆件牛（`cow-sheet.png`），换成抠好透明底的角色真图（正面 `cow-view-1` / 侧面 `cow-view-3`，base64 内嵌，不依赖路径解析）。
  - 牛在底部舞台条内瞬移：消失化形 → 现身气浪圈，落地 **70% 概率正面亮相**，与桌面版/弹窗版行为一致。
  - 待机时上下轻微浮动；上涨触发时**正面朝屏幕跳起来喊 `\牛来/`**，带金光光环。
  - 对话气泡跟随牛的实时位置（水平 + 高度），不再固定死在 160px。
  - 原 `cow-sheet.png` 保留未删，供回滚。

## 1.4.1 (2026-08-20)

- ✨ **瞬移落地正面亮相**：桌宠瞬移现身时 **70% 概率正面亮相**（正对着你），30% 才是侧面；落地后保持该姿势待机，互动/喊牛来/拖拽结束后回到当前待机姿势，不再强制切回侧面。
- 🚀 **弹窗版（`lib/desk-pet/`）升级为瞬移飞行版**，行为对齐 Electron 桌面版：
  - 牛在舞台范围内瞬移飞行（高度可达 82%），带 4 道残影 + 仙气粒子 + 落地气浪圈，同样 70% 正面亮相。
  - 股票上涨触发 **金光爆闪瞬移 + 喊 `\\牛来/`**（8s 节流 + 70% 概率），飞到舞台中上部显眼位置再喊。
  - 新增单击互动（跳跃/压扁/抖动 + 随机语录）、左键拖动、大小档位按钮（小/中/大）。
  - 弹窗改为大屏打开（`screen.availWidth × availHeight`）；保留暂停/刷新控制条与 Demo 模式兜底。
  - 素材复用抠好透明底的 5 视角图（`cow-view-1/3.png`）。

## 1.4.0 (2026-08-20)

- ✨ **满屏飞仙版**：桌宠窗口从固定小窗升级为 **铺满整个工作区的透明层**，牛在全屏幕范围内瞬移飞行（高度可达屏幕 82%），不再是"原地扑腾"。
  - **鼠标穿透**：默认 `setIgnoreMouseEvents(true, { forward: true })`，全屏窗口不挡任何点击；鼠标悬停到牛身上才自动接管，移开即恢复穿透——正常用电脑零干扰。
  - **拖动改造**：左键拖动直接改牛的屏幕坐标（不再移动窗口），拖到哪停到哪。
  - **气泡避让**：牛飞到屏幕上半截时气泡自动挪到脚下，永远不出屏。
  - 大小档位（小/中/大）改为控制牛本体尺寸（110/140/185px 高），窗口恒为全屏；窗口不再占用任务栏（`skipTaskbar`）。
  - 自定义股票弹窗改为屏幕居中弹出。

## 1.3.0 (2026-08-20)

- ✨ **修仙瞬移版桌宠**：淘汰"整图平移式散步"，改为修仙流瞬移。
  - 待机时每隔 2.6~5.2s 在窗口内随机瞬移：消失时带光晕化形，轨迹上留下 **4 道残影** 逐渐消散。
  - 落地爆发 **仙气粒子**（蓝白色）+ 气浪圈扩散；瞬移高度也随机浮动，不再只贴地。
  - 股票上涨喊牛来升级为 **金光爆闪瞬移**：金色粒子 + 金色气浪圈，闪现到屏幕中部跳起喊 `\\牛来/`。
  - 互动语录新增修仙梗（"贫道牛来是也~"、"渡劫中，勿扰"、"本牛已飞升"…）。
- 🐛 **修复气泡文字被窗口裁切**：气泡从 pet 内部移到 body 层，JS 自动收敛在窗口边界内（左右各留 4px），长文字自动换行（`max-width: calc(100vw - 10px)`）。
- 🔍 桌宠窗口尺寸整体加大：小 180×230 / 中 260×330 / 大 340×420（给气泡留显示空间）。

## 1.2.0 (2026-08-20)

- 🐮 **Electron 真·桌面桌宠**：新增 `desktop-pet/` 独立桌面应用。
  - 透明、无边框、始终置顶窗口；角色素材自动抠白底（5 视角全套 `cow-view-1~5.png`）。
  - 左键按住可拖动窗口位置；平时以侧面视角在窗口内来回踱步，方向自动翻转。
  - 股票上涨随机喊 `\\牛来/`（腾讯财经公开接口，默认盯 A 股三大指数；8s 节流 + 70% 概率防刷屏）。
  - 单击角色轮流触发互动：跳跃 → 压扁回弹 → 左右抖动，配随机趣味中文语录气泡（不透明底、位于角色上方）。
  - 右键菜单：选择关注股票（预设白酒/科技/新能源 + 自定义代码）、调整大小（小/中/大）、置顶开关、退出程序。
  - 设置持久化到系统 userData 目录。
- 网页版/弹窗版桌宠立绘同步改为抠好透明底的 5 视角素材。

## 1.1.0 (2026-08-20)

- 🐂 **牛来桌宠 / Cow Pet**：新增一只会撒欢的牛。
  - 两种模式：网页版（DSH 会话页面内浮动）+ 桌面版（`window.open` 弹出的独立无边框 popup）。
  - 复用 `/dshq/quotes` 接口，自动跟随当前自选列表；任一标的 `changePct > 0` 即触发。
  - 牛跑两步（1.6s 横向 78px + 双跳）+ 文字喊两声 `\\牛来/`（320ms 间隔）+ 列出在涨的标的。
  - 同一波上涨节流 1.8s；标的回落即安静漫步。
  - 桌面版 `lib/desk-pet/standalone.html` 同源走 `/dshq/quotes`；脱同源（双击 HTML）进入 Demo 模式提醒从 DSH 启动。
- 头部新增 `🐂 牛来` 切换按钮（与 panel 共享开关状态）。
- 新增资源：`lib/cow-sheet.png`（5 视角立绘，做 sprite）；`lib/desk-pet/`（独立桌面版子目录）。

## 1.0.0 (2026-08-19)

- Initial open-source release.
- A-share default page: Shanghai Composite / Shenzhen Component / ChiNext.
- US default page: Dow Jones / Nasdaq Composite / S&P 500.
- A-share K-lines: intraday (分时), 60-min, daily, weekly, monthly.
- US K-lines: daily, weekly, monthly.
- Per-market watchlist with add/remove, persisted in localStorage.
- Auto-refresh quotes (5 s CN / 10 s US).
- Always-on-top, draggable panel with position memory.
- Zero-config free data sources (Tencent Finance).
